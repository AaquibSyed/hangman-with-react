const initialState = {
  categories: [
    "Hollywood",
    "Bollywood",
    "Food",
    "Countries",
    "Celebrities",
    "Cartoons",
  ],
};

export const hangmanReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
